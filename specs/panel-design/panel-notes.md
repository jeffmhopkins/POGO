# POGO Panel Design Notes — 48HP

## Status: Complete ✅

The panel is fully defined in `tools/panel-data.yaml` (source of truth) and
generated by `tools/build_panel.py`. DRC is clean as of 2026-05-27.

## Quick Reference

| Property | Value |
|---|---|
| Width | 48HP (243.84mm) |
| Height | 3U (128.5mm usable) |
| Panel file (plugin) | `plugin/res/Pogo-source.svg` (generated) |
| Panel file (mfr) | `plugin/res/Pogo.svg` (text-as-paths, via `--mfr`) |
| Interactive viewer | `design/panel-debug.html` (generated) |
| DRC | `python3 tools/build_panel.py --check` |

## Workflow

```bash
# Edit panel layout
edit tools/panel-data.yaml

# Regenerate SVG + debug HTML
python3 tools/build_panel.py

# Check DRC violations
python3 tools/build_panel.py --check

# Preview with layers
open design/panel-debug.html

# Get C++ widget positions for Pogo.cpp
python3 tools/build_panel.py --cpp
```

## Zone summary

See `python3 tools/build_panel.py --list` for current absolute positions.
See `tools/panel-data.yaml` for zone definitions.

## 40HP archive

Old 40HP panel-notes.md and panel.svg are at
`specs/archive/40hp-era-2026-05/panel-notes.md` and `.../panel.svg`.
